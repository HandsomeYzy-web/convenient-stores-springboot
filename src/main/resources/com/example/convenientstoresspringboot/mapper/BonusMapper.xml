<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.convenientstoresspringboot.mapper.BonusMapper">
<!-- 根据会员ID查询积分 -->
<select id="findByMemberId" resultType="com.example.convenientstoresspringboot.pojo.entity.Bonus">
    SELECT * FROM bonus WHERE member_id = #{memberId}
</select>
</mapper>